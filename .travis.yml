language: rust

os:
  - linux

before_script:
  - rustup component add rustfmt

script:
  - cargo fmt --all -- --check
  - cargo build
  - cargo test
  - cargo test --no-default-features

notifications:
  email: false
